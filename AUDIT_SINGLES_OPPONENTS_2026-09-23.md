# 対戦相手の偏り再監査（2026-09-23）

前回の検証では番号の重複・欠落、休み回数、保存・復元を重点確認していた。今回、対戦相手の分布を独立に測定し、休みの公平性だけでは検出できない偏りを確認した。

## 発見した問題

連続休みを避ける判定を対戦相手より優先すると、半数が休む構成で出場グループが固定される。4番号・1コートでは「1対2」と「3対4」が交互に続き、8番号・2コートではグループ外との対戦が発生しなかった。休み回数の差は0〜1であり、従来の公平性テストだけでは検出できなかった。

## 修正

- 休み回数が少ない番号から休ませる制約を守り、その範囲で休みと対戦相手を同時に最適化。
- 1コートや、総当たりを均等な出場単位に分割できる構成では、総当たりを順に配置。奇数番号にも対応。
- その他の構成では複数の表を比較。同一相手の最大回数、全体の対戦回数の偏り、連続節の再対戦、使用コートの偏りの順に選択。
- 指定休みのない新規作成は最大32候補、途中変更・休み指定ありは最大8候補。理論下限に到達すれば早期終了。
- 過去の保存表が新しい総当たり方式と一致しない場合は、実際の対戦履歴を使う。終了済みの節は維持する。
- 対戦相手の固定化を避けるため、連続休みは許容する。画面にも説明を追加。

## 修正前後の実測

各行は20回生成した中の最悪値。参加番号・コート数は固定、休み指定なし。

| 構成 | 修正前の同一相手の最大回数 | 修正後 | 修正前の未対戦組 | 修正後 |
| --- | ---: | ---: | ---: | ---: |
| 4番号・1コート・30節 | 15 | 5 | 4 | 0 |
| 8番号・2コート・15節 | 3 | 2 | 16 | 0 |
| 12番号・4コート・10節 | 1 | 1 | 26 | 26 |
| 12番号・4コート・15節 | 2 | 1 | 10 | 6 |
| 12番号・4コート・20節 | 2 | 2 | 2 | 0 |
| 12番号・4コート・30節 | 3 | 2 | 0 | 0 |

12番号には66通りの対戦がある。4コート×10節では40試合なので未対戦26組、15節では60試合なので未対戦6組は必ず残る。これは節数による制約である。

## 検証方法

`test_singles_opponents.js` が本番HTMLの関数を直接読み込む。1〜4コート・各コートに必要な最低番号数〜12番号・10/15/20/25/30節の160構成を各20回、合計3,200回生成する。休み順と種目を交互に切り替え、固定乱数で再現可能にした。

- 毎節の番号重複・欠落・コート数を検証。
- 全節の休み回数差、対戦回数、未対戦組、連続節での再対戦を集計。
- 同一相手の最大回数の理論下限は「総試合数÷全対戦組数」の切り上げ。公平性や指定条件を無視した下限であり、あらゆる制約下で達成可能とは限らない。
- 総当たりを分割する構成は、未対戦を含む全対戦組の回数差が最大1であることも検証。
- 指定休みで同じ相手との対戦が不可避になる例、旧方式の保存表から続きを生成する例も検証。

最大32候補での最終検証では、3,200ケースすべてで休み回数差は最大1、同一相手の最大回数は上記理論下限に一致した。2番号しかいない構成を除き、連続する節での同一対戦は0件だった。

これは検証した条件・乱数での実測であり、任意の条件・生成結果の完全均等を保証するものではない。休み指定・途中変更では、指定や終了済みの履歴を優先する。コート使用回数や連続休みも完全均等とは限らない。

## 回帰検証

`npm run test:singles` 合格。基本の計算1,938ケース、途中変更300セッション・2,400回、休み指定500パターン、対戦相手の分布3,200ケース、ブラウザ59項目を確認。保存・共有・旧保存データからの再生成・操作キャンセルも合格。既存ダブルス版の計算処理は今回変更していない。
