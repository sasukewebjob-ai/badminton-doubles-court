# シングルス・固定ペア番号版

`singles.html` をブラウザで開きます。通常版の上部にもリンクがあります。

- シングルス：1番号＝1人、2〜12名。
- ダブルス：1番号＝固定ペア1組、2〜12ペア（4〜24名）。ペア内の組み替えはしません。
- 1〜4コート。1コートにつき2番号が必要です。人数不足時は作成できません。
- 10 / 15 / 20 / 25 / 30節。初期設定は4コート・12番号・10節。
- 休み指定がなく参加番号が変わらない間は、休み回数の差は最大1回。
- 各節の出場番号を決めた後、全ての対戦組み合わせを調べて対戦回数の偏りを抑え、同点の候補からコート使用回数の偏りが小さい配置を選びます。全節を通じた最適解や総当たりの完成は保証しません。
- 全員が毎節出場する構成では総当たりを繰り返します（参加番号・コート数を途中変更した時点で周期を再開）。
- 最後の番号から休む設定では、第1節は1対2・3対4…の順（指定休みがある場合は、出場可能な番号の昇順）。
- 休み指定、番号単位の途中参加・離脱・復帰、コート数変更、出場／休み集計、読み上げ、再読込時の復元、閲覧用共有リンク、10節ごとのPNG保存、印刷に対応。
- 番号は1〜12で管理します。参加者変更時は同じ番号を使って復帰できます。新規参加・復帰時は、継続参加者の最小休み回数を計算上の基準にして以後の休みを割り振ります。画面の集計は実際の参加期間の出場／休み回数です。
- 休み指定は公平性より優先。途中変更で指定が参加番号や休み枠に合わなくなる場合は変更を中止し、元の対戦表を保持します。
- 入力欄の変更は「コート割を作成」で反映します。「残りの節を作り直す」は作成済み表の種目・休み指定を引き継ぎます。
- 名簿・男女別コートは通常版のみの機能です。この版は番号で運用します。

通常版 `index.html` の計算処理は変更していません。新しい版は依存ライブラリのない単一HTMLで、保存キーは `badminton-singles-pairs-session-v1`、共有形式は `#sp1=`。通常版の保存キー／共有形式とは別です。閲覧用リンクでは端末の保存データを上書きしません。

共有する場合はWebに配置したページでリンクを作成してください。ローカルのHTMLから作った `file:` リンクは他の端末では使えません。画像保存・読み上げ・Web共有はブラウザの対応状況に依存します。

検証：`npm run test:singles`（計算とブラウザ）、`npm test`（通常版を含む計算回帰）。
