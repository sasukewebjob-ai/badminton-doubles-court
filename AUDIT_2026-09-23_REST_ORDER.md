# 休み順の変更と再監査（2026-09-23 午後）

依頼は「バグやエラー・計算ミス・偏りがないか入念にチェック」と、途中で追加された「シングルス・固定ペア版の休む順番がランダムなので、9ペアなら9番→1番（または1番→9番）の順にしたい。ダブルス版と全く同じでよい」の2つ。

## ダブルス版（index.html）で見つけて直したもの

直す範囲はユーザーが選んだ（A〜Eすべて）。

| | 問題 | 修正 | 実測（修正前→修正後） |
| --- | --- | --- | --- |
| A | 周の切り替わりで、避けられるのに2節続けて休む人が出る（例: 4コート24人10節で毎回4人） | 同じ休み回数の中で、直前の節に休んだ人を後ろへ回す | 休みが半分未満の280構成: 避けられる連続休みがある構成 100→0、途中変更の境目 1,458件中 0件 |
| B | 休みが参加人数の半分以上だと出場する顔ぶれが固定される（例: 2コート24人は1〜8・9〜16・17〜24番の3組のまま） | 休みが半分以上の構成は「出場する順番」で回す。1周目は番号順、2周目以降は周ごとに決まった乱数列で並べ替える | 140構成の一緒に出場しない組の合計 5,844→2,454（2コート24人30節は 192→9）。同じシードの本体生成で重複ペア 217→20、同じ相手の最大回数の合計 120→96 |
| C | 2コートで特定の人がほぼ同じコートに固定される | ペア・対戦の評価が同点の候補の中から、コートの登場回数が偏らないものを選ぶ | 14構成×20シード: コート差の合計 314→269、8割以上同じコートの人 17→9 |
| D | 番号のみモードで離脱者がいる表を再読み込みすると、有効な休み指定が「今は対象外」と赤字表示 | 再読み込み時の入力欄を「作成したときの設定」（人数・コート数）に戻す。再読み込みしない画面と同じ状態 | 再現手順で誤表示が消えることをブラウザで確認 |
| E | 「消去して最初から」の後も「表示中のコート割に反映するには…」が残る | 消去時に注意書きを更新 | 同上 |

トレードオフ（ユーザー承認済みの方針による）:

- Aにより、ユーザーが以前示した9人・休み3人の3周目の例（1,4,7／2,5,8／3,6,9）は、第6節（7,8,9）から7番が続けて休まないよう 1,2,4／5,7,8／3,6,9 に変わる。3周目のどの組にも7〜9番が入るため、組を保ったまま連続休みを避ける方法はない。
- Bでは、休みがちょうど半分の構成（2コート16人・3コート24人）で2節続けて休む人が増える（2コート16人30節: 8→56件。最長は2節のまま）。半分の人が毎節休む構成では、連続休みを0にすると2つの組が交互に出るだけの固定になるため、顔ぶれの多様さを優先した。
- 同じ相手の最大回数などは、生成のたびに乱数で決まる。6〜60シードの比較で悪化する構成は見つからなかった（最悪値が+1に見えた構成も、シードを増やすと差が消えた）。処理時間の増加は最大1割程度（4コート26人30節で約0.13秒）。

## シングルス・固定ペア版（singles.html）

### 休み順の変更（ユーザー要望）

最初に、休みを番号順に厳守すると、1コートや2コート8番号以上では対戦相手が近い番号に固定されることを試算で示した（例: 2コート8番号は28組中16組が一度も当たらない）。ユーザーの選択は「偏る構成以外はダブルス版と同じ」。

- 休みが参加番号の半分未満（4コート全部、3コート7〜11番号、2コート5〜7番号、1コート3番号）: ダブルス版と全く同じ休み順。index.html の `makeStrideGroups`〜`generateRestSchedule` をそのまま写し、`test_singles_rest_order.js` が一字一句の一致と、全構成の休み表の一致を確認する。9ペア・4コートなら 9→8→…→1→（2周目はずらす）。休み指定・途中参加の後回し・直前の休みの後回しもダブルス版と同じ。
- 休みが半分以上（1コート全部、2コート8番号以上、3コート12番号）: 1周目（全員が1回休むまで）だけ番号順。途中参加者は最初からの番号の後ろ。1周目の中に休み指定がある番号は、その節を自分の番とする。2周目以降は従来どおり休みと対戦相手を同時に選ぶ。
- 引き換え: 休みが先に決まるため、一部の構成で同じ相手との最大対戦が理論下限より1回多く、一度も当たらない組が最大2組多くなる（各20回の実測で、2コート7番号、3コート10・11番号の15〜30節、4コート12番号15節）。候補を512個まで増やすと多くは下限に届くが、1回の作成が0.4秒程度（スマホでは数秒）になるため、候補数は32のまま。

### 監査で見つけて直したもの

途中変更を2,400回ランダムに行い、同じ対戦が2節続く件数を原因別に数えた。

| 原因 | 件数（修正前→後） | 対応 |
| --- | --- | --- |
| 全員出場の区間に切り替えた直後、総当たりが直前の節と同じ対戦から始まる（例: 4コート8番号へ変更） | 104→0 | 直前の節と重ならない位置から総当たりを始める。開始位置は残した節から決まるので、作り直しても総当たりが続く |
| 休みを先に決めた構成で、対戦回数の均等を優先して直前と同じ相手を選ぶ | 49→0（変更前の方式では2） | 直前の節と同じ対戦は、ほかの組み合わせがある限り選ばない。新規作成の結果は変わらない（もともと0件） |
| 2番号だけ／休みの公平で出場者が決まる1コート | 避けられない | 仕様 |

## 検証

- `npm test`（ダブルス版10スイート、回帰、`test_rest_rotation.js`、シングルス版の計算・監査・対戦分布・`test_singles_rest_order.js`）
- `npm run test:stress`、`npm run test:browser`（13ファイル。`test_forced_note_browser.js` を追加）
- 追加したテストは、修正前のコードで実行して不合格になることを確認した（A・B・C・D・E、シングルス版の休み順・総当たり開始位置・2節連続の同じ対戦）。
- `test_algorithm.js` の表示だけの休み順チェック2件（N=6休み3、N=9休み3）の期待値を新しい休み表に合わせた（終了コードには影響しない表示）。

実機（iPhone Safari／Android）の共有・音声・画像保存は今回も未検証。シングルス版の画像保存は、通常版がiPhoneで問題になった `a.download` による保存のみで、プレビュー表示の代替がない（未確認の懸念）。
