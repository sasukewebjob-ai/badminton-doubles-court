# コート割アプリ 検証記録（2026-09-07）

対象はこのフォルダーの `index.html`。計算処理、変更履歴、保存・共有の整合性を調査し、再現できた不具合を修正した。公開サイトへの反映は実施していない。

## 修正した不具合

| 問題 | 再現条件と影響 | 修正 |
| --- | --- | --- |
| メンバー変更を繰り返すと途中参加者の休みが集中 | 2コート・10人・30節で10節終了後に11番を追加し、11節終了後に再度組み直す。11番に与えた休み2回分の補正が消え、第12・13節などで連続して休む | 参加・離脱・復帰の履歴を順にたどり、過去の補正を含む割当用の休み累計を再構築。画面の休み回数は実績のまま |
| コート数変更後・最終節の変更後に共有リンクが開けない | 第3節終了後に4→3コートへ変更、または全10節の第9節終了後に追加。正常なデータを共有の検証処理が拒否 | `atRound` を「変更後の最初の節」として統一し、最終節からの変更を許可 |
| 共有データの途中区間の参加者不整合を見逃す | 途中参加した当節の番号を離脱者へ置き換えても受け入れていた | 全節について、その時点の在籍者・コート数・休み・出場者が一致するか検証 |
| 保存データの一部破損で復元処理が停止 | 節が `null`、選手が重複、変更履歴が `null` など。配列の外形確認だけで不正な状態を採用していた | 状態を採用する前に内容を検証し、不正な保存を読み込まない。古い形式の任意項目欠落には対応 |
| 出入りを繰り返し番号が200を超えると共有できない | 2コート・26人・30節で8人ずつ交代を繰り返す。同時参加人数は26人以内でも累計番号は218になる | 番号上限を共有形式が実際に表現できる1071へ一致させた。同時参加人数の上限26人は維持 |
| 不成立の再生成で内部の節数だけ更新される | 既存10節の状態で30節を選択し、人数不足などで生成が中断 | 生成が成立した時点で内部の節数を更新 |

これまでの `test_algorithm.js` は、計算処理を約500行複製して検証していた。`test_app_loader.js` で本体のスクリプトを直接読み込む方式へ変更し、本体の変更をテストが取り逃さないようにした。Nodeテストで置き換えるのは画面描画・保存・ダイアログなどのブラウザ入出力であり、計算関数は本体を実行する。

## 検証範囲

- 既存の計算テスト全10スイート。2〜4コート、最大26人、10・15・20・25・30節の225構成×5試行＝1,125件の基本スイープに加え、休み順、固定初節、途中変更、復帰、種目別、休み指定を確認。
- 追加の休み計算10,000ケース、合計154,258節。欠番、偏った初期回数、途中参加者、休み指定を組み合わせ、休み人数・重複・指定順守・非指定枠の公平性を毎節確認。
- 3・4コートで可能な全30通りの男女比×30節＝900節。全員男性／全員女性を含め、男女枠、ミックスペア、番号の網羅性、履歴の合計と対称性を確認。
- 100シナリオ×9回＝900回のメンバー変更。追加、離脱、復帰、コート数変更を混在させ、独立して累計する休み補正との一致、消化済み節の不変、共有の往復一致を確認。
- 本体の生成・変更処理を直接呼ぶ7件の回帰テスト。200番を超える交代を含む。
- 既存ブラウザ10ファイルと追加回帰1ファイル。375px幅、名前、種目表示、読み上げ、保存・復元、共有、休み指定、画像の画素・見切れ・Canvas上限を確認。追加回帰では6種類の破損データ、古い保存形式、リロード後の休み補正を確認。

検証環境は Windows、Node.js v22.18.0、Playwright 1.63.0、既存のChromium（chromium-1223）。iPhone実機のSafariでの操作・音声・画像保存は今回の検証対象外。ペアや対戦の偏りは確率的な最適化なので、全乱数で理論最適になることまでは保証していない。

## 再実行

```powershell
npm test
npm run test:stress
# このPCの既存ブラウザを使う場合
$env:PLAYWRIGHT_CHROMIUM='C:\Users\hanim\AppData\Local\ms-playwright\chromium-1223\chrome-win64\chrome.exe'
npm run test:browser
```

`npm run test:all` で計算・回帰・追加負荷検証・ブラウザ検証を順に実行できる。テスト依存関係は `package-lock.json` に記録した。

実行結果ログはローカルの `audit_all.log` と `audit_regression_browser.log` に保存。追加ブラウザテストの初回は、旧形式のテストデータに保存日時を付け忘れ、復元バナー待機でタイムアウトした。テストデータを修正して追加ファイルを再実行し、終了コード0を確認した。
