# システムレビュー結果

確認日: 2026-07-24
対応日: 2026-07-25（4件すべて修正済み）

## 状態

通常利用で再現する重大な不具合は確認されていない。
レビューで挙がった4件は 2026-07-25 にすべて修正した。

## 見つかった課題

### 1. 共有リンクの内容検証が不足 → 対応済み（2026-07-25）

- 重要度: 中
- 対象: `index.html` の `decodeShareData()`
- 仕様外の「5コート・31節・各節0コート」という共有データが正常データとして受理された。
- 現在はデコード後に、参加者と節が空でないことしか確認していない。
- 細工された共有リンクで大量の節・コートを指定すると、DOM描画の負荷増加、画面停止、Canvas生成失敗につながる可能性がある。

対応内容:

- `validateSharedSession()` を追加し、デコード直後に構造と範囲を検証する（不正なら従来どおり `null` を返して警告→通常モード）。
- コート数2〜4、節数10〜30、同時参加26人まで、番号は1〜200（`SHARE_LIMITS`）。
- 現役・のべ参加者の番号は重複なし、`maxNumber` は のべ参加者の最大値と一致、現役はのべに含まれること。
- 変更履歴は節番号が非減少かつ最終節未満、コート数2〜4、番号はのべ参加者の範囲内。
- 各節は「節数＝totalRounds」「節番号が連番」「コート数が変更履歴どおり」「同じ人が二重登場しない」「最後の変更より後の節は現役全員がちょうど登場」を検証。
- 名前・ゲスト性別のキーものべ参加者の範囲内に限定。

回帰テスト: `test_share.js` に [7] を追加（23→44項目）。レビュー指摘の payload を含む15の細工パターンを拒否、正常データ3種は従来どおり通過、細工リンクを実際に開いても閲覧モードにならないことを確認。

### 2. ブラウザテストが特定PCの絶対パスに依存 → 対応済み（2026-07-25）

- 重要度: 中
- 対象: Playwrightを使用する各テストファイル
- PlaywrightとChromiumの参照先が `C:/Users/hanim/...` に固定されている。
- 現在のPCでは実行できるが、別PCやCI環境ではそのまま実行できない。
- `package.json` とローカル依存関係が存在しない。

対応内容:

- `package.json` を追加（Playwrightを `devDependencies`、`npm test` / `npm run test:browser` / `npm run test:all` ほか個別スクリプト）。
- `pw.js` を追加し、Playwrightの解決を1か所に集約。優先順位は ①プロジェクトの `node_modules` ②環境変数 `PLAYWRIGHT_MODULE` ③開発機のグローバル。ブラウザ実行ファイルはフォールバック時のみ既知のパスを使い、`PLAYWRIGHT_CHROMIUM` で上書き可能。
- Playwrightを使う9ファイルすべてを `require('./pw')` ＋ `chromium.launch(launchOptions())` に統一。
- `run_browser_tests.js` を追加し、ブラウザ検証8ファイルを一括実行して総合結果と終了コードを返す。
- `.gitignore` に `node_modules/` を追加。

### 3. アルゴリズムテストの一部が終了コードに反映されない → 対応済み（2026-07-25）

- 重要度: 中
- 対象: `test_algorithm.js`
- `totalErrors`、`changeErrors`、`revErrors`、`fixErrors`、`retErrors`、`defErrors` は結果表示だけで、失敗時に `process.exitCode = 1` が設定されない。
- これらの検証だけが失敗した場合、コマンドが終了コード0になり、CIなどが合格と誤判定する可能性がある。

対応内容:

- `recordSuite(name, errors)` を追加し、9スイートすべてのエラー数を `SUITE_RESULTS` に集約。1件でもあれば `process.exitCode = 1`。
- 末尾に「総合結果」ブロックを追加し、スイート別のエラー数と合計を表示。

確認: 通常実行は exit 0（全9スイート合格）、`changeErrors` に人為的エラーを注入したコピーは exit 1 になることを実測。

### 4. 本番確認スクリプトが最新の26人対応を判定していない → 対応済み（2026-07-25）

- 重要度: 低
- 対象: `check_deploy.js`
- 現在の新版判定は名簿、画像保存修正、ミックス半々バランスの存在を確認している。
- 人数セレクトの上限や生成上限が26人であることは確認していないため、24人上限の旧版でも合格する可能性がある。

対応内容:

- 4コート選択時の人数選択肢が16〜26人の11件であることを確認。
- 26人×4コート×10節を実際に生成し、毎節4コート・休み10人になることを確認。
- 名簿モードの確認前に保存データを消して読み直すようにし、両検証が干渉しないようにした。
- 判定結果を終了コードに反映（`selOk && gen26 && namesOk && imgOk`）。

確認: 現行の本番URLに対して実行し、5項目すべてOK。

## 今回合格した検証

- アルゴリズム検証: 1125件合格
- ブラウザ回帰検証: 184件合格
- 性別端数構成ファズ: 30構成×300試行合格
- 26人生成、永続化、共有、名簿、音声、種目別コート、画像保存: 合格
- 公開版: 26人対応、名簿生成、画像保存を確認

## 修正後の検証（2026-07-25）

- `node test_algorithm.js`: 全9スイート合格・exit 0（人為エラー注入時は exit 1）
- `node run_browser_tests.js`: 8ファイル全合格（persistence 29／share 44／names 32／speech 29／gender 42／bugfix 9／26players 14／fixed_round1 6 ＝ 205項目）
- `node check_deploy.js`: 本番URLで5項目OK

## 備考

- このファイルはレビュー時点の課題メモ。
- 修正した課題には、対応日、コミット、追加した回帰テストを追記する。
