# シングルス・固定ペア番号版の不具合確認（2026-09-23）

## 再現して修正した問題

1. **途中変更を繰り返すと、先に参加した番号の休み補正が失われる**
   - 再現例：10番号・4コートで開始 → 第10節後に12番号へ増員 → 第15節後に3コートへ変更。
   - 修正前は第17節で、参加時点の補正を含む休み回数に2回の差が発生。
   - 保持した節を読み直す際、過去の各参加・復帰時点の補正も再現するよう修正。

2. **同じ参加番号・コート数で残りを作り直すと、総当たりが最初に戻る**
   - 再現例：8番号・4コートで第3節まで終了後、そのまま残りを再生成。
   - 修正前は第4節で、総当たりの一巡前に対戦相手が重複。
   - 現在の構成が始まった節を特定し、その総当たりの続きを使うよう修正。

3. **変更後に「終了した節」が第1節へ戻る**
   - 連続操作で終了済みの節まで作り直す原因になる。
   - 終了位置を保存・復元・共有データに保持するよう修正。
   - 旧形式の保存データは、最後の参加番号／コート数変更位置を初期値に使用。

## 新版の検証

- 基本の計算・復元検証：1,938ケース。
- 固定乱数による途中変更：300セッション、計2,400回。終了済みの節の保持・番号の重複／欠落・参加時の補正を含む休み公平性・共有往復を検証。
- 休み指定：500パターン、各30節。指定休みと全番号の配置を検証。
- 新版のブラウザ検証：59項目。320 / 375 / 600px幅、PNG保存、読み上げ文、共有、再読込、旧データ復元、キャンセル、保存容量不足、保存禁止、クリップボードの代替処理を含む。
- `test_singles_audit.js` は修正前に失敗することを確認した再現テストを含む。

## 検証範囲

ローカルファイルをNode.jsとPlaywrightのChromiumで検証。スマートフォンの画面幅は模擬検証であり、iPhone Safari／Android実機の共有画面・音声・ファイル保存までは検証していない。Web公開作業は行っていない。

以前に作成した表の内容は自動変更しない。修正された計算を既存の表に反映する場合は、終了位置を確認して残りの節を作り直す。

## 既存版を含む回帰結果

- `npm test`：既存版の計算10スイート、既存不具合の回帰検証、新版の計算・追加監査がすべて合格（終了コード0）。
- `npm run test:browser`：既存版を含む全12ファイルが合格（終了コード0）。
- その後追加したキャンセル・保存不可などのテストを含む新版の最終ブラウザ検証も、59項目すべて合格。
- `git diff --check`：差分の空白エラーなし。
