# プロジェクト概要
バドミントンのダブルス試合で、各節のコート割（ペア・対戦相手・休み）を自動生成するWebアプリ。

## 目的・ゴール
バドミントンクラブ等で、ダブルスマッチを公平に組む。
- 節数を 10 / 15 / 20 / 25 / 30 から選択可（デフォルトは10節。実運用の最頻構成に合わせて2026-07-05変更）
- 休み回数を均等化（差は最大1回を保証）
- 休み順を選択可（1番から ／ 最後の番号から。デフォルトは最後の番号から。2026-07-05変更）
- 「最後の番号から休む」選択時は第1節のみ番号順の固定配置（A: 1,2 vs 3,4 / B: 5,6 vs 7,8 …）。受付順にそのまま並べられる。第2節以降は通常の最適化
- 同じペア・同じ対戦の偏りを最小化（150試行のランダム最適化＋上位5候補のスワップ局所探索）
- 対戦相手の偏り抑制：超線形ペナルティ (回数+1)² でいろんな相手と当たるように分散
- メンバーの途中追加・離脱・復帰に対応（消化済み節は不変、履歴を引き継いで残りを再生成。離脱者は同じ番号で復帰可）
- 固定名簿（KALIDIA members.txt と共有・26名）からの参加者選択＋番号ランダム自動割当（じゃんけん代替）。全表示が「番号 名前」になり、番号表カードも表示。名簿未選択なら従来の番号のみモード
- 各節の音声読み上げ（節カードの🔊ボタン。ブラウザ内蔵TTSでAPI不要・オフライン動作。名簿モードはフリガナ`KANA`で読み、番号のみモードは「5番」読み。実機で不自然だった人は`SPEECH_KANA`で読み上げ表記だけを手動上書き。閲覧モードでも使用可）
- localStorage永続化（リロード・タブ落ちでも状態を復元。復元バナー＋消去ボタン、再生成時は上書き確認）
- 対戦表の共有リンク（URLにセッションを埋め込み。開いた人は閲覧モードで同じ対戦表が見える。サーバー不要）
- 種目別コート（3・4コート×名簿モード限定のオプション。4コート: A=男子ダブルス／B=女子ダブルス／C=ミックス／D=なるべくミックス、3コート: A=男子／B=女子／C=なるべくミックス。男女比が偏る節はA→Bの優先で維持し余りを最後のコートが吸収。休み公平は最優先で不変。ミックス出場が特定の人に偏らないよう、個人単位でも自種目コートと半々に近づける＝2026-07-22追加）
- スマホで誰でもすぐ使える

## 技術スタック
- HTML / CSS / JavaScript（単一ファイル、外部依存なし）
- モバイル端末（iPhone/Android）での利用を想定
- ライブラリ・フレームワーク未使用

## やらないこと・制約
<!-- 必要に応じて更新 -->
- プレイヤー名管理（番号のみで識別：1番、2番…）
- 結果のエクスポート/保存（毎回その場で生成）
- コート数 2〜4、参加人数 最大26人（2026-07-24に24→26へ拡張）、節数 10〜30

## デザイン方向性
- スマホ縦持ち最適化（max-width 600px）
- 明るく親しみやすいグラデーション基調（緑×水色）
- カードUIで節ごとに視認性を確保
- スティッキーヘッダーで統計表が見やすい

## ファイル構成
- `index.html` … 本体（GitHub Pages のエントリポイント）
- `package.json` … テスト実行用（`npm test`＝アルゴリズム、`npm run test:browser`＝ブラウザ一括、`npm run test:all`＝両方。Playwrightはdev依存）
- `pw.js` … Playwrightの参照を集約するヘルパー。①プロジェクトのnode_modules ②`PLAYWRIGHT_MODULE` ③開発機のグローバル の順で解決し、実行ファイルは`PLAYWRIGHT_CHROMIUM`で上書き可
- `run_browser_tests.js` … ブラウザ検証8ファイルを一括実行して総合結果と終了コードを返す
- `test_algorithm.js` … アルゴリズム検証用テスト（Node.jsで実行）
- `test_persistence.js` … localStorage永続化のブラウザ検証（Playwright、375px幅）
- `test_share.js` … 共有リンクのブラウザ検証（Playwright、ホスト/閲覧者を別コンテキストで）
- `check_deploy.js` … デプロイ後に本番URLで動作確認するスクリプト（Playwright）
- `check_fixed_round1.js` … 第1節固定配置のブラウザ検証（Playwright、ローカルindex.html）
- `test_names.js` … 名前付き参加者機能＋画像Canvas上限のブラウザ検証（Playwright、375px幅・dpr3）
- `test_speech.js` … 音声読み上げのブラウザ検証（Playwright、speechSynthesisをスタブ化して読み上げテキストを検証）
- `experiment_fixed_round1.js` … 第1節固定が第2節以降の公平性に影響しないことを実測した比較実験（`--precise`で主要構成×20試行）
- `test_bugfix.js` … バグ修正の回帰テスト（最終節直前のメンバー変更で全節消失しない／共有リンクの名前XSS対策。2026-07-06）
- `test_gender.js` … 種目別コートのブラウザ検証（Playwright、375px幅。表示条件・テンプレート準拠・共有v3・男女別ゲスト）
- `test_image.js` … 画像保存の画素レベル検証（Playwright、375px幅・dpr3。描画したCanvasを直接読み、見切れ・はみ出し・面積上限を確認）
- `test_26players.js` … 26人対応のブラウザ検証（Playwright、375px幅・dpr3。上限セレクト・生成・公平性・画像保存・27人目拒否）

## 公開URL
- https://sasukewebjob-ai.github.io/badminton-doubles-court/
- リポジトリ: https://github.com/sasukewebjob-ai/badminton-doubles-court

## 検証状況
1125テスト合格（225構成 × 5試行。2026-07-24の26人対応で975/195構成から拡大）。詳細は `test_algorithm.js`。
- 休み均等化（差≤1）: 全構成で達成
- 第1節ランダム化: OK
- ペア偏り最小化: 4c×20p×30節で全ペア解消、20節以下は理論最小値
- 対戦相手分散（2026-06改善）: 4c×20p×15節で最大対戦が3回→2回・未対戦5.8→1.6組、4c×16p×10節は理想分布（全員と対戦・最大2回）を達成。評価式は ペア重み1000 + 対戦(c+1)² の超線形ペナルティ
- メンバー途中変更（2026-06追加）: 7シナリオ合格（消化済み節不変・離脱者非登場・ペア厳守・休み公平差≤1・二重変更・コート数変更）。途中参加者の休みは「現時点の最少回数」扱いで以降公平。Playwrightで画像保存のマーカー描画も確認済み
- スマホUI修正（2026-06-10）: 追加人数セレクトが横にはみ出す問題を修正。選択肢ラベルを範囲表記（例: +8人（25〜32番））に短縮し、`.input-row select` に `min-width: 0` を追加。375px幅・24人構成で横スクロールなしをPlaywrightで確認済み
- localStorage永続化（2026-06-11）: 生成・メンバー変更のたびに `session` を保存、起動時に復元。Playwright検証17項目合格（生成→リロード復元・変更マーカー復元・消去→復元なし・再生成の上書き確認・壊れたデータ耐性・375px横はみ出しなし）。キーは `badminton-court-session-v1`
- 休み順の逆順化（2026-06-11）: 「休み順」セレクトで「最後の番号から休む」を選択可。`generateRestSchedule` の第6引数 `reverse` で並び位置の基準を降順に反転するだけ（公平性ロジックは共通）。検証: 逆順=通常の鏡像（108構成）・差≤1（180構成）・欠番あり・途中変更後も引き継ぎ・リロード復元（Playwright 5項目）すべて合格。旧保存データは `restOrder` 無し→「1番から」扱い
- 第1節固定配置（2026-06-11）: 「最後の番号から休む」時のみ、新規生成の第1節を番号順固定（A: 1,2 vs 3,4 …）。`generate()` 内で `r === 0 && restOrder === 'desc'` のとき `assignCourts`/`balanceCourts` を通さず固定配置（履歴には通常どおり登録）。メンバー変更は消化済み最小1節なので第1節再生成は起きない。事前実験（experiment_fixed_round1.js、主要10構成×20試行）で第2節以降の公平性指標（maxペア・重複ペア・max対戦・未対戦）に有意差なしを確認。検証: Node 4項目（固定一致・第2節ランダム維持・ascランダム維持・休み差≤1）＋Playwright 6項目（desc固定・休み[17,18]・リロード復元・asc非固定）全合格、既存975テストも回帰なし
- 対戦表の共有リンク（2026-06-11）: 「🔗 対戦表のリンクを共有」ボタンでセッション全体（rounds/changes/players/restOrder）をURLハッシュ `#s=...` に埋め込んだリンクを作成。スマホはWeb Share API（LINE等の共有シート）、PCはクリップボードコピーにフォールバック。エンコードは独自の可変長整数（0-47は1文字、48-1071は2文字、base64url文字のみ）＋作成時刻5文字で、15節構成でも400文字前後。リンクを開いた人は閲覧モード（`viewerMode`）：閲覧バナー（作成時刻表示＋「自分のコート割を作る」ボタン）、入力・メンバー変更UI非表示、localStorage非保存（閲覧者自身の保存データを壊さない）。壊れたリンクは警告して通常モードへ。hashchangeでリロードするので開いたままのタブに新リンクが来ても反映。検証: test_share.js 23項目合格（往復一致・別ブラウザで内容一致・閲覧者のlocalStorage非汚染・再共有反映・自分のデータへの復帰・壊れたリンク耐性・375px）＋既存回帰（persistence 29・fixed_round1 6）合格
- 離脱者の同番号復帰（2026-06-11）: 離脱中の人がいるとメンバー変更UIに「復帰する人」チップ（緑）が出る。復帰者は同じ番号でペア・対戦・休みの履歴を引き継ぐ。休み回数は `max(自身の履歴, 現役の最少回数)` で引き継ぎ、復帰直後に休みが集中しないようにする。変更履歴は `change.returned`（旧データは `|| []` でガード）。検証: 基本復帰・同時変更（復帰＋離脱＋追加）・復帰→再離脱・休み引き継ぎ（Node 4シナリオ）＋チップ表示/復帰反映/リロード復元（Playwright 7項目）すべて合格

- iPhone画像ぼやけ修正（2026-07-02）: 画像保存でiPhoneだけ文字が読めないほどぼやける不具合を修正。原因はCanvas総ピクセル数がiOS Safariの上限16,777,216pxを超過していたこと（旧ガードは67Mまで許容、典型構成のdpr3で30〜45Mpxを生成→iOSが描画バッファを縮小）。`MAX_AREA`を16,777,216に変更し全端末共通に。10節2コートで2503×6701px（幅2500px≒十分な可読性）。Playwright（dpr=3）で上限内を確認済み
- 名前付き参加者機能（2026-07-02）: 固定名簿`ROSTER`（index.html内定数、KALIDIA members.txt と共有。名簿変更時は両方更新）から参加者をチップで選択→生成時にFisher-Yatesで番号をランダム割当（`session.names` = {番号:名前}）。表示は`pname()`（名簿モード「1 田中」／番号のみ「1番」）で全箇所統一（対戦表・休みバッジ・番号表カード・休み回数表・メンバー変更チップ・変更マーカー・画像）。途中追加は名簿チップ（名前付き）＋ゲスト人数（番号のみ）の併用可。共有リンクはバージョン'2'で名前ブロック（JSON→UTF8→base64url）を末尾付加、番号のみは従来の'1'のまま（後方互換）。画像は番号表カードを先頭に描画し、名前が長い行はフォント自動縮小（休みバッジは入り切らなければ番号のみに退避）。検証: test_names.js 32項目合格＋既存回帰（algorithm全緑・persistence 29・share 23・fixed_round1 6）合格

- 音声読み上げ（2026-07-02）: 各節カードの🔊ボタンでその節の対戦を読み上げ（Web Speech API＝ブラウザ内蔵TTS、API代不要・通信不要）。読み上げ文は`roundSpeechText()`（「第N節の対戦を読み上げます！Aコート、たなか、すずき、たい、こばやし、はら。やすみ、よだ。」冒頭フレーズは2026-07-06ユーザー要望）。冒頭は**漢字かな交じり**で渡す：ひらがな＋数字ベタ書き（だい1せつ…）はTTSの形態素解析が効かず「だい・いち・せつ」と不自然なアクセントになるため、漢字で「だいいっせつ」と正しく読ませる（2026-07-06アクセント修正）。名前部分は従来どおりフリガナ。名簿モードは`KANA`（名前→ひらがな対応表、ROSTERとセットで更新）で読み、対応がなければ漢字のままTTSへ。番号のみモードは「5番」読み。読み上げ中は「⏹ 停止」表示で再タップ停止、別の節タップで切替。閲覧モード（共有リンク）でも使用可。再描画時は`stopSpeech()`。速度は`rate=0.89`（0.9→0.99→0.89とユーザー要望で調整。最終2026-07-06に10%ダウン）、音量は`volume=1.0`＝API上限（端末のメディア音量を超える増幅は仕様上不可。体育館ではBluetoothスピーカー推奨）。iPhoneは消音スイッチONだと音が出ない点に注意。検証: test_speech.js 28項目合格（KANA完全性・読み上げ文・フリガナ使用・停止トグル・節切替・番号のみ・閲覧モード）＋既存回帰（algorithm全緑・names 32・persistence 29・share 23・fixed_round1 6）合格

- 種目別コート（2026-07-20追加）: 3・4コート×名簿モード時のみ「種目別コート」セレクトが出現（デフォルトoff。選択肢の文言はコート数連動: 4c「A男子・B女子・C/Dミックス」／3c「A男子・B女子・Cミックス」）。onで A=男子ダブルス／B=女子ダブルス／C=ミックス／D=なるべくミックス（3コートはA男子/B女子/Cなるべくミックス、12〜15人向け。理想は毎節男6女6出場。同日追加）。性別は`GENDER`定数（原本members.txtの[男性]/[女性]セクションからupdate_members.pyが自動生成。ROSTER/KANAと同時同期）。各節の出場者の男女数から`courtTemplates`が各コートの男女枠を決定（理想 A男4/B女4/C・D男2女2。不足はA→B→C優先で維持し余った性別で埋める＝実質Dが吸収）。男女混在コートのペアは`pairTypesFor`で戦力が偏らないよう男女ペア優先（男3女1→男男vs男女）。ペア組みはタイプ別クォータつき貪欲法（クォータ合計は男女数と常に一致し詰みなし）、コート割当はA→D順に対戦回数最少の組、局所探索は同性同士の入替のみ許可。種目別モード時は第1節固定配置と通常のbalanceCourtsをスキップし、代わりに`balanceCourtsSameType`で「同じ男女構成のコート同士だけ」を過去のコート登場回数（二乗ペナルティ）で入れ替える（例: 10男6女ならA/D両方が男子→ローテーション、理想構成ならC/Dのミックス同士。2026-07-20追加。これにより同種目コートへの分散を保証：8男8女×15節でミックス出場が全員7〜8回、10男6女で男性のA/D登場差最大2回）。休み割当は完全に不変（公平差≤1最優先。男女比が偏ると構成が崩れる節を許容し、infoBoxで男6人/女6人未満を事前警告）。ゲスト追加は男女別セレクト（`session.guestGenders`に記録）。共有リンクはバージョン'3'（{n:名前,g:ゲスト性別}ブロック。v1/v2は後方互換）。表示は節カードとCanvas画像に種目タグ（男子=青/女子=ピンク/ミックス=紫/混成=緑。実際の構成から判定するので崩れた節は実態どおり表示。**最後のコート（4cのD／3cのC）はミックスにならなかった節は種目に関わらず緑タグ**＝人数調整コートだと分かるように。男3女1等の3対1構成は「混成」ラベル。2026-07-20ユーザー要望）。コート数の途中変更は3⇔4なら種目別を維持、2コートへ変更した区間は通常生成に戻る。検証: test_algorithm.jsに4コート7シナリオ（8:8理想・休みあり10:10・男過多10:6・女過多4:12・奇数9:9・途中追加ゲスト・途中離脱）＋3コート6シナリオ（6:6理想・13人/15人休みあり・男過多8:4・女過多4:8・途中追加・4c→3c変更、全てテンプレート準拠＋休み差≤1）＋分散検証7シナリオ（全員が全コートに登場・同種目コートのローテーション・A/D（3cはA/C）登場差・ランダム性。3回連続実行で安定確認。3c理想構成はミックス出場が全員5回ちょうど）＋test_gender.js 42項目（表示条件・コート数連動文言・タグ・タグ色・D/C緑タグ・リロード復元・共有v3閲覧者・男女別ゲスト・3コート版・オフ/番号のみ非影響）全合格。既存回帰全緑（algorithm総エラー0・names 32・persistence 29・share 23・fixed_round1 6・speech 29・bugfix 9）。8男8女×10節でペア重複ゼロ（max1）を確認
- 途中追加者の休み順修正（2026-07-16）: 「最後の番号から休む」（desc）でメンバーを途中追加すると、追加者が最大番号ゆえに休みキューの先頭に割り込み、追加直後の節でいきなり休みになる問題を修正（実運用報告: 第4節から14番追加→第4節で即休み）。`generateRestSchedule`に第7引数`deferredNew`（途中追加者の番号）を追加し、追加者が初休み前（休み回数が参加時のまま）の間は同回数グループ内の並びを最後尾に回す（重み2）。初休み直後の節は連続休み回避で後ろへ（重み1・軟制約）。既存メンバーの残りキューが1節に収まる場合（例: 4c×20人+1人→休み5人/節）は同節末尾に入るのが正しい動作。一度休んだら通常のストライド順に復帰、公平性（差≤1）ロジックは不変。`applyMemberChange`が`newNumbers`を渡す（復帰者は対象外・従来どおり）。検証: test_algorithm.js に5シナリオ追加（報告シナリオdesc・asc・複数人追加・2周目途中追加・大人数）全合格＋既存回帰全緑（algorithm総エラー0・bugfix 9・names 32・persistence 29・share 23・fixed_round1 6・speech 29）
- ミックス/自種目コートの半々バランス（2026-07-22追加）: 実運用（9男8女×10節・途中参加2人）で「女子ダブルス7回・ミックス系2回」の個人偏りが出た報告を受け、評価関数に第3項を追加。各自の「ミックス系（男女混在）コート出場 − 自分の種目コート出場」の差`mixDiff`を履歴管理（`updateMixDiff`で毎節更新、メンバー変更時は消化済み節から再構築。過去在籍者の性別は`genderOfNum`で解決）し、配置後の差の二乗×`MIX_BALANCE_WEIGHT`(=12)をスコアに加算。150試行＋局所探索（同性入替）が自然にミックス枠の人選をローテーションさせる。重みはW=0/4/8/12/20の比較実験で決定：W=12で差スプレッドが男最大10→おおむね2・女6→おおむね2、ペア重複は悪化なし（8男8女×10節でmax1維持）、対戦3回組が平均4.6→9.4組の軽微な悪化は許容（ユーザー優先度判断）。9男8女×10節の再現実験で全女性が女子D5回/ミックス4回の完全均一を確認。検証: test_algorithm.jsに実運用再現シナリオ（15人3c→+男1・4cへ→+女1、全節テンプレート準拠＋休み差≤1＋差スプレッド男女≤5）と分散検証8（9男8女×10節、差スプレッド男≤5・女≤4）を追加、8男8女×15節・3c6男6女×15節のミックス出場差の閾値を6→3に強化（実測: 4cは最小7最大8、3cは全員5）。既存回帰全緑（algorithm 975＋総エラー0・gender 42・names 32・persistence 29・share 23・fixed_round1 6・speech 29・bugfix 9）＋画像保存の再確認6項目OK（種目別17人×10節で例外なし・失敗アラートなし・Canvas面積15,090,645px≤上限9割・プレビュー表示）
- スマホで画像保存が無反応になる問題の修正（2026-07-22）: 実機スマホで「📷 対戦表を画像として保存」を押しても何も起きない報告。原因は`saveAsImage`がCanvas面積をiOS上限16,777,216pxちょうどに張り付ける設計（10〜15節はSCALE=2で常時上限到達）で、実機のメモリ状況次第で描画バッファ無効化や`toBlob`のnull返しが起き、`URL.createObjectURL(null)`の例外で**ダウンロードもプレビューも出ず無反応**になっていたこと（エラー表示も無かった）。対策: ①`MAX_AREA`に1割の余裕（×0.9。幅2161px≒可読性は維持）②描画を`draw(dpr)`に分離し、描画後に`getImageData`1pxでバッファ生存確認→失敗なら解像度を0.7倍→等倍で再試行 ③`toBlob`がnullなら`toDataURL`でプレビュー表示に再挑戦 ④それでも失敗時はalertで通知（無反応の禁止）⑤プレビューのblob URLを60秒revokeから「次回保存までは生存」に変更（時間を置いた長押し保存の失敗を防止）⑥`a.click()`失敗（アプリ内ブラウザ等）でもプレビューは表示。検証: フォールバック9項目（通常保存・面積9割以内・toBlob nullでdataURL・1回目失敗で縮小再試行・全失敗でalert・連続保存でURL生存）＋既存回帰全緑（algorithm総エラー0・names 32・gender 42・persistence 29・share 23・fixed_round1 6・speech 29・bugfix 9）
- バグ修正2件（2026-07-06）: ①最終節の直前（消化済み=総節数-1）でメンバー変更すると、消化済みセレクトが空になり、そこで再度「作り直す」を押すと`parseInt('')=NaN`→`remaining<=0`ガードを素通り→`slice(0,NaN)=[]`で全節消失していた不具合を修正。`applyMemberChange`に有効節番号ガード（要素null含む）を追加し、作り直せる節が無いときはUIを「これ以上作り直せません」の案内に置換してボタン自体を出さない。②`renderResults`が`pname()`（共有リンク経由で他人が細工可能な名前を含む）を`innerHTML`に無エスケープで埋め込んでいたXSS入口を、`escHtml()`導入で全注入箇所エスケープ。`changeLabel`はCanvas描画と共用のため関数内では変換せず注入側でエスケープ。検証: test_bugfix.js 9項目合格＋既存回帰全緑（algorithm・names 32・share 23・persistence 29・fixed_round1 6・speech 28）

- 26人対応（2026-07-24追加）: 参加人数の上限を24→26人に拡張（番号のみモード含む全モード。ユーザー要望）。変更はindex.htmlの上限4箇所のみ（人数セレクト上限・infoBox警告・生成時alert・メンバー変更時alert）で、公平性ロジック（ペア重複重み1000・対戦(c+1)²・balanceCourtsのコート分散）は既存のまま26人でもそのまま機能。事前調査（30試行×7構成の実測）でコート登場差4が出る頻度（約1%の人）は24人以下と同等＝26人化による品質劣化なしを確認。検証: test_algorithm.jsの全スイープ上限を26に拡大（195→225構成、975→1125テスト全合格）＋26人専用6シナリオ（4c×26p×10/15/30節・4c×25p・3c×26p・2c×26p: 休み差≤1・maxペア1・max対戦≤3・コート差≤4）＋test_26players.js 14項目（セレクト16〜26・毎節休み10人・ペア重複なし・375px横はみ出しなし・dpr3画像保存15.1Mpx≤上限9割・リロード復元・27人目は「上限は26人です」で拒否・25人生成）。既存回帰全緑（names 32・persistence 29・share 23・fixed_round1 6・speech 29・bugfix 9・gender 42）

- システムレビュー指摘の修正4件（2026-07-25）: `REVIEW_FINDINGS.md`（2026-07-24のレビュー）の課題を全件修正。①**共有リンクの内容検証**＝`decodeShareData()`のあとに`validateSharedSession()`を追加。細工リンクで「5コート・31節・各節0コート」等の仕様外データが受理され、描画負荷やCanvas生成失敗につながる余地があった。コート数2〜4・節数10〜30・同時26人・番号1〜200（`SHARE_LIMITS`）に加え、番号の重複なし／`maxNumber`＝のべ参加者の最大値／変更履歴の節番号が非減少かつ最終節未満／各節のコート数が変更履歴どおり／同じ節に二重登場なし／最後の変更より後の節は現役全員がちょうど登場／名前・ゲスト性別のキーがのべ参加者の範囲内、まで検証。不正なら従来どおり`null`→警告して通常モード。②**テストの絶対パス依存を解消**＝`package.json`＋`pw.js`＋`run_browser_tests.js`を追加し、Playwrightを使う9ファイルを`require('./pw')`／`chromium.launch(launchOptions())`に統一（別PC・CIでも`npm install`で動く）。③**test_algorithm.jsの終了コード**＝`recordSuite()`で9スイートのエラーを集約し、1件でもあれば`process.exitCode=1`＋末尾に総合結果を表示（従来はtotalErrors等6スイートが表示だけで、失敗してもexit 0だった）。④**check_deploy.jsの26人判定**＝4コートの人数選択肢が16〜26人（11件）であることと、26人×4コート×10節で毎節4コート・休み10人になることを追加確認（24人上限の旧版を掴んだら落ちる）。検証: test_share.js 23→44項目（レビュー指摘payloadを含む15の細工パターン拒否＋正常3種は通過＋細工リンクを開いても閲覧モードにならない）、test_algorithm.js は通常exit 0／人為エラー注入でexit 1を実測、ブラウザ回帰8ファイル205項目全合格（persistence 29・share 44・names 32・speech 29・gender 42・bugfix 9・26players 14・fixed_round1 6）、check_deploy.js を本番で実行し5項目OK

- 画像保存の追加検証（2026-07-25）: 「他にバグはないか」の確認として`test_image.js`を新設。従来のテストは「例外が出ない・Canvas面積が上限内」までしか見ていなかったため、`document.createElement`をフックして**描画済みCanvasを直接取得し画素を解析**する方式にした。確認項目は ①アラートなしで保存完了＋プレビューがCanvasと同寸法 ②面積が上限の9割以内・幅1000px以上 ③下端90〜98%に濃い画素がある＝最後の節が見切れていない ④休みバッジの左端が画像幅の18.6%以上＝「第N節」と重ならない（`maxBadgeW = cardW - 110*SCALE` に対応。SCALEが約分されるので比率で判定できる）⑤右端2.5%に文字画素ゼロ＝カード外にはみ出していない。11構成（2c8人〜4c26人30節、名簿26人×2c＝休み18人、種目別3c、変更マーカー最長、連続保存、共有リンクの閲覧モード）で**75項目全合格＝画像保存に不具合は見つからなかった**。テスト初回に出た4件のNGはすべてテスト側の問題（種目別の緑タグを休みなしバッジと誤検出→左隣がヘッダー紺色かで判別、生成時の案内アラートを数えていた、プレビュー更新前の旧imgを読んでいた）で、アプリ側の欠陥ではない。残存リスクのうち「`toBlob`がコールバックを返さない端末では無反応のまま」は同日対応（下記）。「画像の高さが最大12,623px＝古いiOS12以前の4096px制限には非対応」は未対応だが、その場合も無反応ではなくアラートが出る
- `toBlob`無応答の監視タイマー（2026-07-25）: 画像保存の最後の無反応ルートを塞いだ。7/22の修正は`toBlob`が**nullを返す**ケースと例外は塞いだが、**コールバック自体が返らない**端末では待ち続けて何も起きないままだった。`settled`フラグ＋`setTimeout(15000)`で見切り、`toDataURL`に切り替えてプレビューを表示する（両方走っても`settled`で二重表示なし）。15秒は重い端末の正常なPNGエンコードより長めに取った値。検証: `test_image.js`に`HTMLCanvasElement.prototype.toBlob`を無応答にスタブするケースを追加（16.1秒でプレビュー表示・アラートなし）、全78項目合格

- iPhoneで保存できない問題の修正＋画像の10節分割（2026-07-25）: 実機報告「20節だと画像が保存できない。『このサイトが外部アプリケーションを開こうとしています』で許可しても何も起きない。長押しなら保存できるが文字がかなり小さい」。**原因はCanvas生成ではなくダウンロード処理**で、iOS Safariは`<a download>`に対応しておらず blob URL を外部アプリ起動として扱うだけだった（描画は成功していたので長押し保存はできていた）。文字が小さいのは20節を1枚にすると1462×10318px＝**縦横比7.1倍**の細長い画像になり、画面に収めると縮むため。対応: ①`IMAGE_CHUNK_ROUNDS=10`で**10節ごとに分割**（15節→10+5の2枚、20節→2枚、25/30節→3枚、10節以下は従来どおり1枚）。ボタンも「📷 1枚目を保存（第1〜10節）」のように枚数分並べる（ユーザー選択: 1枚ずつボタン）。各画像は自己完結（タイトルに「（第11〜20節）」の範囲＋番号表＋その範囲の変更マーカー。`prevCourts`はチャンク開始前の変更を先に適用してから描画）②`isIOSDevice()`のときは`a.click()`を**呼ばない**（あの紛らわしいダイアログを出さない）③`navigator.canShare({files})`が使える端末はプレビュー上部に「📤 写真に保存 / 共有」ボタンを出し、新しいタップで`navigator.share`を呼ぶ（gesture切れを避けるため保存処理とは分離）④iOSではプレビュー直下に「画像を長押しして『"写真"に追加』」の案内を常時表示。結果: 20節が2枚×2042×7393px（幅+40%・縦横比3.62）になり文字が約2倍の大きさに。検証: `test_image.js`を分割対応に拡張し13構成194項目全合格（ボタン数＝ceil(節数/10)・各枚の面積/幅/縦横比≤5・見切れ・はみ出し・実機報告の4c20人20節を番号のみ／名簿の両方で追加）＋ブラウザ回帰9ファイル全合格

- 休みバッジを中央寄せ（2026-07-25・ユーザー要望）: 節ヘッダーの休みバッジが右寄せだったのを「第N節」の右側の空きスペースの中央に変更（画面・保存画像の両方）。画面は`.rest-badge`に`margin: 0 auto`（flexの自動マージンで空きを均等配分。長いときは従来どおり左右いっぱいに広がる）。画像は`badgeX`を右端固定から`areaL〜areaR`（areaR=カード右端−14*SCALE、areaL=areaR−maxBadgeW＝「第N節」と重ならない左限界）の中央に。入り切らない長さのときは従来どおり右寄せにフォールバックするので、`test_image.js`の「バッジ左端≧画像幅の18.6%」チェックはそのまま有効（194項目全合格）

- 名簿更新＋読み上げ表記の上書き（2026-08-13・ユーザー要望）: 原本`members.txt`に**上條**（男性・かみじょう）を追加、**柴**（女性）を削除（計26名のまま、男性14→15・女性12→11）。`update_members.py`で3アプリへ同期。あわせて実機で不自然だった2名の読み上げを修正: **バレ**＝`ばれ`だと平板で「バレる」のように聞こえる→カタカナ`バレ`で頭高に、**濱島**＝`はましま`だとスマホで頭の「は」が飲まれ「マシマ」に聞こえる→カタカナ`ハマシマ`。実装は`SPEECH_KANA`（index.html内の**手動管理**テーブル）を新設し、`sname()`が`SPEECH_KANA[名前] || KANA[名前] || 名前`の優先順で参照する。`update_members.py`の置換対象は`const KANA = \{.*?\};`なので**名簿同期で消えない**（別名の定数にしたのはこのため）。読み上げ専用＝画面・保存画像の表示は従来どおり漢字のまま。候補の決定は聞き比べページ`speech-test.html`を一時デプロイして実機で選定（バレ5案／濱島6案×「先頭」「2番目」の位置違い）→採用後に削除（履歴に残るので再利用可）。検証: test_speech.js 29→40項目（SPEECH_KANAのキーがROSTER在籍・値が空でない・両名が新表記で読まれる・旧表記では読まれない・画面に読み上げ用カタカナが漏れない・上書きの無い人は従来どおりフリガナ）。**上書きを外したコピーで該当4項目が落ちることを実測**＝テストが実際に効いていることを確認済み
  - 同時に直した既存テストの不備2件（いずれも**テスト側の問題**でアプリの欠陥ではない）: ①`test_speech.js`の「漢字ではなくフリガナで読む」は`names.every(nm => !text.includes(nm))`で判定していたが、**バレは名前自体がカタカナ＝読み上げ表記と同一**なので原理的に成立しない。「出場者全員が読み上げ表記で登場する」＋「表記が名前と異なる人だけ漢字で登場しない」に変更。②`test_gender.js`が名簿チップを「index 0〜13＝男性・14〜＝女性」と直書きしており、男性15・女性11に変わって狙った男女比が崩れ6項目が失敗した。`pickByGender(page, 男数, 女数)`で`GENDER`から並び位置を引くよう変更し、**今後名簿の男女構成が変わっても壊れない**ようにした（42項目全合格に復帰）

## 関連メモ
- KALIDIA関連リポジトリ: 名簿 `sasukewebjob-ai/kalidia-meibo`／コート割 `sasukewebjob-ai/kalidia-court`
- 名簿の同期（2026-07-06変更）: 原本は `バドミントン系/members.txt`（フリガナ付き）。`バドミントン系/update_members.py` を実行すると本アプリの `ROSTER`・`KANA` 定数を含む3アプリへ一括反映される。手動同期は不要になった
