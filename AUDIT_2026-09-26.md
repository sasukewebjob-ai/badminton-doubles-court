# バグ・エラー・計算ミスの再監査（2026-09-26）

依頼は「作業再開。現状バグ、エラー、計算間違えがないか確認」。既存テスト（`npm test`・`npm run test:stress`・ブラウザ14ファイル）は修正前の時点で全合格だったため、テストが拾っていない不具合を、ダブルス版の計算・ダブルス版の画面・シングルス版の3観点でコードを精読して探した。休み回数差が2以上になる、同じ節に同じ番号が二重に出る、表が壊れる、画面が止まる、といった重いものは無かった。見つかった11件はユーザーが全件修正を選んだ。

## ダブルス版（index.html）

| | 重大度 | 問題 | 修正 |
| --- | --- | --- | --- |
| D1 | 中 | 2回目の途中変更（何も変えない作り直しを含む）で、前回途中参加してまだ休んでいない人が休み待ちの先頭に割り込み、参加直後に休む（例: 4コート20人で第1節後に21番、第2節後に22番を追加 → 21番が第3節で休み）。7/16の修正が今回の追加者にしか効いていなかった | `applyMemberChange` が `deferredNew` に「以前の変更で参加し、参加後まだ休んでいない継続者」も含める |
| D2 | 中 | 休みが参加人数の半分を超える構成で、同じ人が長く続けて休む（2コート26人30節で6節、2コート20人10節で4節。避けられない長さはそれぞれ3・2節）。9/23の修正Bの副作用 | 半分を超える構成で、連続休みが「避けられない長さ」を超えた人を同じ休み回数の中で先に出場させる（下記） |
| D3 | 中 | 名簿モードで途中変更後に再読み込みすると、コート数は作成時・名簿チップは現在のメンバーに戻り「4コートなのに15人」で作成できない。休み指定も「今は対象外」になる（修正Dの名簿モード版） | 名簿チップを作成時のメンバー（途中参加者を除くのべ参加者）に戻す |
| D4 | 低 | 指定休みの人が離脱して `*` が1つも無いのに、画面に凡例だけ出る | `hasForcedRests()` を「実際に `*` が付く節があるか」に変更（保存画像と同じ判定） |
| D5 | 低 | 読み上げ中に「消去して最初から」を押しても止まらない | `clearSavedSession()` で `stopSpeech()` |
| D6 | 低 | 画像を作っている途中に作り直すと、作り直す前の表の画像がプレビューに出る | 作成開始時の `lastRounds` と違えば、その画像は表示もダウンロードもせず捨てる |
| D7 | 低 | 手を加えた共有リンクで同じ節に変更が2件あると検証を通り、変更帯が1件分しか出ない | 変更の節番号を厳密に増加で検証（画面の操作では最初の版から同じ節に2件は作れないので、正規の保存データは影響なし） |
| D8 | 低 | 共有リンクの名前が `toString` などだと、読み上げに関数の文字列が入る | `ownValue()` で名簿テーブルは自前のキーだけ引く（`sname`・`genderOfNum`） |

### D2の方式の選定

休みが半分を超える56構成×節数5×休み順2で比較（`generateRestSchedule` 単体、決定的）。

| 案 | 連続休みの最長が避けられない長さを超えた分の合計 | 一緒に出場しない組の合計 |
| --- | --- | --- |
| 修正前 | 186 | 2,272 |
| 連続休みの長い人から出場（下限ちょうど） | 16 | 5,830（2コート24人30節で 9→192＝顔ぶれ固定に逆戻り） |
| 下限に達した人を先に出場 | 16 | 4,654 |
| **採用: 下限+1節を超えそうな人を先に出場** | 112（全構成で最長≦下限+1） | 2,354（+4%） |

ちょうど半分の構成（2コート16人等）は対象外。そこで連続休みを抑えると2つの組が交互に出るだけの固定になるため（9/23のトレードオフのまま）。

## シングルス・固定ペア版（singles.html）

| | 重大度 | 問題 | 修正 |
| --- | --- | --- | --- |
| S1 | 中 | 「=」がすべて `%3D` に変わった共有リンク（接頭辞 `#sp1=` を含む）が開けない。9/23の修正は後半の「=」だけで、テストも接頭辞を変換対象から外していた | 起動時に `#sp1%3D` を `#sp1=` に読み替える |
| S2 | 低 | `Array.prototype.at` の無いiOS 14.0〜15.3で、保存データの復元と共有リンクが必ず失敗する | `s.rounds[s.rounds.length-1]` に置換 |
| S3 | 低 | 保存が使えない端末で「消去して最初から」が効かず、起動のたびに「復元できませんでした」と出る | 消去は保存の成否にかかわらず画面を消す／読み込み自体の失敗は「保存なし」扱い |
| D1 | 中 | ダブルス版D1と同じ（以前参加した番号は一度でも登場すると後回しの対象外） | 最初の節にいなかった番号で、まだ休んでいないものも後回しの対象にする |

休み順の関数（`generateRestSchedule`）はD2の変更を index.html からそのまま写した（`test_singles_rest_order.js` の一字一句一致チェック合格）。

## 検証

- 回帰テスト: `test_audit_2026_09_26.js`（計算10項目、`npm test` に追加）、`test_audit_2026_09_26_browser.js`（画面29項目、`npm run test:browser` に追加）
- 修正前のコードで実行し、11件すべてで該当テストが不合格になることを確認した（計算7件・画面16件）
- `npm test`、`npm run test:stress`、ブラウザ15ファイル全合格。精読で作った網羅テスト（2〜4コート・最大26人・名簿/種目別/休み指定・1ケース1〜4回の途中変更、1,200ケース）も違反0件

直さなかった細部（シングルス版）: 保存画像に指定休みが無くても「* は指定休み」と出る／固定ペアのとき画像の一部が「1番ペア」でなく「1番」表記。実機（iPhone Safari／Android）は今回も未検証。
